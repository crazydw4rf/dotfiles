# dotfiles

## How to install

### With stow

```bash
stow -v .

# for symlinking dot files in home directory
stow -vt ~ dots
```

### With just

Make sure to install `stow` first.

```bash
just install
```
